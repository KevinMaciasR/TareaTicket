{% extends 'base.html.twig' %}

{% block title %}Usuario{% endblock %}

{% block body %}
    <h1>Usuario</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Usuario</th>
                <td>{{ usuario.usuario }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_control2_us_index') }}">back to list</a>

    <a href="{{ path('app_control2_us_edit', {'usuario': usuario.usuario}) }}">edit</a>

    {{ include('control2_us/_delete_form.html.twig') }}
{% endblock %}
