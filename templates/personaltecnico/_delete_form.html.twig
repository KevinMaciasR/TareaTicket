<form method="post" action="{{ path('app_personaltecnico_delete', {'idTecnico': personaltecnico.idTecnico}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ personaltecnico.idTecnico) }}">
    <button class="btn">Delete</button>
</form>
