{% extends 'base.html.twig' %}

{% block title %}Factura{% endblock %}

{% block body %}
    <h1>Factura</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdFactura</th>
                <td>{{ factura.idFactura }}</td>
            </tr>
            <tr>
                <th>IdFacturador</th>
                <td>{{ factura.idFacturador }}</td>
            </tr>
            <tr>
                <th>IdCliente</th>
                <td>{{ factura.idCliente }}</td>
            </tr>
            <tr>
                <th>Tipotrabajo</th>
                <td>{{ factura.tipotrabajo }}</td>
            </tr>
            <tr>
                <th>Precio</th>
                <td>{{ factura.precio }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ factura.fecha ? factura.fecha|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_factura_index') }}">back to list</a>

    <a href="{{ path('app_factura_edit', {'idFactura': factura.idFactura}) }}">edit</a>

    {{ include('factura/_delete_form.html.twig') }}
{% endblock %}
