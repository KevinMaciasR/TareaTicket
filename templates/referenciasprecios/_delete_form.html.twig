<form method="post" action="{{ path('app_referenciasprecios_delete', {'idReferencia': referenciasprecio.idReferencia}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ referenciasprecio.idReferencia) }}">
    <button class="btn">Delete</button>
</form>
