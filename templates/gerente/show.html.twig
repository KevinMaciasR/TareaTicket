{% extends 'base.html.twig' %}

{% block title %}Gerente{% endblock %}

{% block body %}
    <h1>Gerente</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idgerente</th>
                <td>{{ gerente.idgerente }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ gerente.nombre }}</td>
            </tr>
            <tr>
                <th>Cedula</th>
                <td>{{ gerente.cedula }}</td>
            </tr>
            <tr>
                <th>Usuario</th>
                <td>{{ gerente.usuario }}</td>
            </tr>
            <tr>
                <th>Clave</th>
                <td>{{ gerente.clave }}</td>
            </tr>
            <tr>
                <th>Correo</th>
                <td>{{ gerente.correo }}</td>
            </tr>
            <tr>
                <th>Rol</th>
                <td>{{ gerente.rol }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_gerente_index') }}">back to list</a>

    <a href="{{ path('app_gerente_edit', {'idgerente': gerente.idgerente}) }}">edit</a>

    {{ include('gerente/_delete_form.html.twig') }}
{% endblock %}
