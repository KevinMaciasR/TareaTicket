{% extends 'base.html.twig' %}
{% block title %}Inicio de sesion{% endblock %}
{% block body %}
<div class="container">
    <div class="row">
        <div class="login-form bg-light mt-4 p-4">
            <form method="post" action ="Controller/ClienteController" class="row g-3">
                <h1 class="h3 mb-3 font-weight-normal">Incie sesion</h1>
                <div class="col-12">
                    <label for="inputEmail">Usuario</label> {# for debe tener el nombre del id de input#}
                    {#por medio del ID me enlazo a la variable; el name lo usamos en el codigo php para tomar el valor del dato igresado#}
                    <input type="text" name="user" id="inputEmail" class="form-control" required autofocus> 
                </div>
                <div class="col-12">
                    <label for="inputPassword">Contraseña</label>
                    <input type="password" name="password" id="inputPassword" class="form-control" required>
                </div>
                <div class="col-12">
                    <a class="btn btn-lg btn-primary float-end" href="{{path('app_cliente_inicio')}}">
                        Iniciar
                    </a>
                    {#<a class="btn btn-primary float-end" >Iniciar</a>            #}
                </div>
            </form>
        </div>
        <a class="btn btn-secondary" href="{{path('app_cliente_new')}}">Registrarse</a>
        <a class="btn btn-secondary"  href="{{ path('app_usuario') }}">Regresar</a>
    </div>
    {#<a href="{{ path('app_cliente_show', {'idCliente': cliente.idCliente}) }}">show</a>#}
</div>
{% endblock %}